function drawslabpartdg32(index, source) {
    let areashapeindex = shape.areaid + "" + index;
    let tmpshapeouterg = mslabpartsdg.select("#slabpartouter" + areashapeindex);
    if (!tmpshapeouterg.empty()) { tmpshapeouterg.selectAll('*').remove(); }
    setshapegroups31(index, 'slabpartdg', 'drawslabpart');
    shapesides(source + ':drawdigslabpart32');
  //  transformshape();   
}
